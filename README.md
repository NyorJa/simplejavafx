# Energy Project

## A project implemented on JavaFX

## Prerequisites
1. JDK 19
2. Maven 3++
3. IDE your choice (personally I use Intellij)

## How to run
1. Clone the project
2. Execute `mvn clean compile` in the root of the project
3. After compile execute `mvn javafx:run`
